1) Sequência de Fibonacci e verificação de presença
Aqui está o código em Python que calcula a sequência de Fibonacci e verifica se o número informado está nela.


def is_fibonacci(number):
    a, b = 0, 1
    while a <= number:
        if a == number:
            return f"O número {number} pertence à sequência de Fibonacci."
        a, b = b, a + b
    return f"O número {number} NÃO pertence à sequência de Fibonacci."

# Defina o número que você deseja verificar
num = int(input("Informe um número: "))
print(is_fibonacci(num))
